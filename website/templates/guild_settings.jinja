{% extends "base.jinja" %}

{% block title %}
MarriageBot - {{ guild.name }} Settings
{% endblock title %}

{% block head %}
<link rel="stylesheet" type="text/css" href="/static/css/guild_settings.css" />
{% endblock head %}

{% block content %}
<div class="content-header-data">
    {% if guild.icon %}<img alt="The guild icon for {{ guild.name }}" src="https://cdn.discordapp.com/icons/{{ guild.id }}/{{ guild.icon }}.png" />{% else %}<img alt="The default guild icon" src="/static/images/GuildDefaultImage.png" />{% endif %}
    <h2>{{ guild.name }} - {% if gold is none %}Gold Settings{% else %}Settings{% endif %}</h2>
</div>
<div class="content-header-data">
    {% if normal == true %}<h2>This guild has <a href="/guild_settings?guild_id={{ guild.id }}">MarriageBot</a> - click for settings</h2>{% elif normal == false %}<h2><a href="/r/invite">Click here</a> to invite normal MarriageBot for this guild</h2>{% endif %}
    {% if gold == true %}<h2>This guild has <a href="/guild_gold_settings?guild_id={{ guild.id }}">MarriageBot Gold</a> - click for settings</h2>{% elif gold == false %}<h2><a href="/buy_gold?guild_id={{ guild.id }}">Click here</a> to buy <a href="/blog/gold">MarriageBot Gold</a> for this guild</h2>{% endif %}
</div>
<form id="prefix" class="boxed">
    <p>Bot prefix - the prefix used to activate the bot on your server.</p>
    <p><input name="prefix" value="{{ prefix }}" placeholder="m!" maxlength="30" /></p>
    <p><button type="submit" formmethod="post">Submit Prefix Change</button></p>
</form> <!-- prefix -->
{#
<form id="blacklisted-channels">
    <p>Blacklisted channels - the channels where you don't want the bot to run commands</p>
    <span style="display: flex; flex-wrap: wrap;">
        <style>
        #blacklisted-channels .col {
            width: 32%;
        }

        @media screen and (max-width: 576px) {
            #blacklisted-channels .col {
                width: 49%;
            }
        }
        </style>

        {% for channel in channels %}
        <p class="col" style="margin: 6px 0;"><input type="checkbox" name="blacklisted-channel" value="{{ channel.id }}">#{{ channel.name }}</p>
        {% endfor %}
    </span>
</form>
#}
{% endblock content %}
